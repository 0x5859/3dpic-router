"""Minimal helpers shared across the 3D-SiNIC routing test suite.

This package is a trimmed shim from the full batch-experiment harness; the
published release ships only ``crossings.py`` (the geometric intralayer
crossing counter) because the parity tests in ``code/tests`` depend on it.
"""
